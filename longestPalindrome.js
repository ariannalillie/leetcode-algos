const longestPalindrome = (s) => {
  // base case when there is only 1 char, that is the answer
  if (s.length === 1) return s;
};

const s = "babad";
console.log(longestPalindrome(s)); // Output: "bab"

